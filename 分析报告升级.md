  Claude-Mem 配置分析报告

  当前版本

  - 已安装版本: 8.5.1
  - 最新版本: 8.5.9
  - 安装时间: 2026-01-03

  v8.5.9 更新内容

  新增 上下文头部时间戳 功能：
  - 显示上下文生成的具体时间
  - 格式示例: [claude-mem] recent context, 2026-01-04 2:46am EST
  - 支持终端彩色输出和 Markdown 格式

  ---
  数据存储位置 (需保留)

  | 路径                            | 大小 | 说明                |
  |---------------------------------|------|---------------------|
  | ~/.claude-mem/claude-mem.db     | 12M  | 主数据库 (SQLite)   |
  | ~/.claude-mem/claude-mem.db-shm | 32K  | 数据库共享内存      |
  | ~/.claude-mem/claude-mem.db-wal | 964K | 数据库写前日志      |
  | ~/.claude-mem/vector-db/        | 114M | 向量数据库 (Chroma) |
  | ~/.claude-mem/settings.json     | 4K   | 配置文件            |
  | ~/.claude-mem/sync-config.json  | 4K   | 同步配置            |
  | ~/.claude-mem/logs/             | 2.4M | 日志 (可选保留)     |

  ---
  需要清理的文件

  | 路径                                       | 大小 | 说明             |
  |--------------------------------------------|------|------------------|
  | ~/.claude/plugins/cache/thedotmack/        | -    | 插件缓存 (8.5.1) |
  | ~/.claude/plugins/marketplaces/thedotmack/ | 232M | Marketplace 文件 |
  | ~/.claude/plugins/installed_plugins.json   | -    | 安装记录         |
  | ~/.claude/plugins/known_marketplaces.json  | -    | Marketplace 记录 |

  ---
  升级步骤建议

  # 1. 备份数据库 (重要!)
  cp -r ~/.claude-mem ~/.claude-mem.backup

  # 2. 停止 worker 进程
  kill $(cat ~/.claude-mem/worker.pid) 2>/dev/null

  # 3. 卸载旧版本 (在 Claude Code 中执行)
  > /plugin uninstall claude-mem

  # 4. 清理 marketplace
  rm -rf ~/.claude/plugins/marketplaces/thedotmack
  rm -rf ~/.claude/plugins/cache/thedotmack

  # 5. 重新添加 marketplace 并安装最新版
  > /plugin marketplace add thedotmack/claude-mem
  > /plugin install claude-mem

  # 6. 重启 Claude Code

  需要我帮你执行升级吗？


   ---
  Claude-Mem 工作原理

  自动化流程

  1. 启动 Claude Code → 自动注入最近 10 个会话的上下文
  2. 正常工作 → 每个工具执行都被捕获
  3. Claude 完成响应 → Stop hook 自动生成并保存摘要
  4. 下次会话 → 之前的工作出现在上下文中

  捕获的工具

  Read、Write、Edit、Bash、Glob、Grep 等所有 Claude Code 工具

  处理后提取的信息

  - Title / Subtitle / Narrative
  - Facts (关键学习点)
  - Concepts (标签分类)
  - Type (decision/bugfix/feature 等)
  - Files (涉及的文件)

  会话摘要内容

  - Request (你的请求)
  - Investigated (Claude 探索了什么)
  - Learned (关键发现)
  - Completed (完成了什么)
  - Next Steps (下一步)

  渐进式披露 (节省 token)

  | 层级    | 内容                         | Token 成本 |
  |---------|------------------------------|------------|
  | Layer 1 | 索引显示 (标题 + token 估算) | 50-200     |
  | Layer 2 | 按需详情 (mem-search skill)  | 100-500/条 |
  | Layer 3 | 完整召回 (读取源文件)        | 按需       |

  ---
  现在请执行安装命令：

  /plugin marketplace add thedotmack/claude-mem
  /plugin install claude-mem

  然后重启 Claude Code 会话即可。

 Claude-Mem 配置总结

  配置文件位置

  ~/.claude-mem/settings.json (首次运行自动创建)

  你当前的配置 (已保留)

  {
    "CLAUDE_MEM_MODEL": "claude-sonnet-4-5",
    "CLAUDE_MEM_CONTEXT_OBSERVATIONS": "50",
    "CLAUDE_MEM_WORKER_PORT": "37777",
    "CLAUDE_MEM_CONTEXT_FULL_COUNT": "5",
    "CLAUDE_MEM_CONTEXT_SESSION_COUNT": "10"
    // ... 其他设置
  }

  核心配置项

  | 设置                             | 你的值            | 说明               |
  |----------------------------------|-------------------|--------------------|
  | CLAUDE_MEM_MODEL                 | claude-sonnet-4-5 | 处理观察的 AI 模型 |
  | CLAUDE_MEM_CONTEXT_OBSERVATIONS  | 50                | 注入的观察数量     |
  | CLAUDE_MEM_WORKER_PORT           | 37777             | Worker 服务端口    |
  | CLAUDE_MEM_CONTEXT_FULL_COUNT    | 5                 | 展开详情的观察数   |
  | CLAUDE_MEM_CONTEXT_SESSION_COUNT | 10                | 拉取观察的会话数   |

  Web 配置界面

  安装后访问 http://localhost:37777 可使用可视化设置界面，支持实时预览。

  v5.4.0+ 变化

  - 使用 mem-search skill 替代 MCP tools
  - 节省 ~2,250 tokens/会话
  - 无需额外配置，自动可用

  ---
  现在请执行安装：

  /plugin marketplace add thedotmack/claude-mem
  /plugin install claude-mem

  然后重启 Claude Code。你的 settings.json 会被保留，新版本会自动使用现有配置。

> 请问 这两个命令不是在 claude code 对话中完成的吧 

⏺ 你说得对！这两个命令 正是在 Claude Code 对话中执行的。

  /plugin 是 Claude Code 的内置斜杠命令，需要你直接在当前对话中输入：

  /plugin marketplace add thedotmack/claude-mem

  等待完成后：

  /plugin install claude-mem

  就像你输入 /help 或 /clear 一样，直接在这个对话窗口中输入这些命令即可。

  执行完成后重启 Claude Code 会话。
